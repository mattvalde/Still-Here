To choose the site backgrounds, replace these files with your own images:

- `background-light.svg`
- `background-dark.svg`

You can use `.jpg`, `.png`, `.webp`, or `.svg`. If you change the file names, update `--background-light` and `--background-dark` near the top of `styles/styles.css`.
